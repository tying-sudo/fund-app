# 后端架构优化：新平台首条指令

将以下内容原样发送给新 AI 平台。第一阶段只允许审计和本地验证；不得直接修改后端、迁移、部署或发布。

```text
项目：tying-sudo/fund-app
工作目录：D:\fund-app
任务：为基金应用制定并分阶段实施后端架构优化方案。在保持现有 Web、Android、基金估值、持仓、OCR、市场数据和 valuation-grid 行为兼容的前提下，提升数据正确性、可靠性、性能、可观测性、安全性和可维护性。

已知结构：Vue 3/Vite 前端在 `src/`；Node/Express fund-proxy 在 `server/`；服务端包含 PostgreSQL/Supabase 迁移、Redis/内存/磁盘缓存、外部数据源聚合和 API；Python/FastAPI 网格在 `vendor/valuation_grid/`；Android 由 Capacitor 管理。它们可独立发布，不能假定可一起重启或一起覆盖。

不可突破的边界：
- 先执行 `git status --short --branch` 并保留所有既有改动、运行时数据和未追踪文件。禁止 reset、checkout、clean、批量格式化和未授权上游同步。
- 不得读取、回显、上传或提交 `.env*`、Cookie、用户持仓、数据库/Redis 备份、服务账户/令牌、SSH 私钥、APK 签名材料和真实生产配置。
- 不得发布前端、后端、grid 或 APK；不得修改 DNS、Nginx、systemd、定时器、生产数据库或 Redis；不得改变版本号。所有生产操作等待独立、明确授权。
- 数据库迁移仅允许新增文件；不得修改已部署迁移、破坏性 DDL、清空数据、重建用户或改变生产 RLS/权限，除非后续获得逐项批准。
- 不得将官方净值、实时估值、缓存快照和市场休市状态混为一谈；不得伪造当日估值。任何数据源调整必须检查真实响应内容、字段语义、北京时间、交易日/交易时段、超时、空响应与降级路径。HTTP 200 不证明数据有效。
- `vendor/valuation_grid/` 是带本地定制的集成上游模块；不得覆盖其 `src`、运行时数据或安装自动同步任务。Python 测试必须从该目录运行。

第一阶段任务：只读审计和本地验证，不写业务代码、不创建迁移、不修改 API 契约。
1. 阅读 README.md、docs/AI_HANDOFF.md、docs/WORKLOG.md、package.json、server/package.json、server/、scripts/、vendor/valuation_grid/ 和现有测试。
2. 在不读取密钥的前提下运行并报告：`npx.cmd vue-tsc --noEmit`、`npm.cmd run build`、相关前端测试、`npm.cmd --prefix server test`；网格测试在 `vendor/valuation_grid` 运行。若命令不可安全运行，说明原因而不是绕过。
3. 追踪前端到后端的实际契约：所有 `/api/`、`/v1/`、OCR、发布元数据、估值、净值、持仓、行情、缓存和网格调用。列出调用方、鉴权、参数、返回、来源、缓存、超时、错误与降级。
4. 审计服务边界：Node fund-proxy、PostgreSQL/Supabase、Redis、Python grid、Nginx/反向代理、Android 远程更新、部署脚本和定时任务。明确哪些是代码事实，哪些需要生产确认。

第一阶段交付物：创建 `docs/backend-architecture-audit.md`，并在回复中摘要说明：
1. 当前架构图和数据流，标出浏览器/Android、前端、Node、Python、数据库、Redis、外部数据源和公网入口之间的信任边界。
2. 接口与数据契约清单，特别是时间、缓存和错误降级语义。
3. P0/P1/P2 风险：密钥与权限、输入校验、OCR 上传、限流、CORS、SSRF/代理、RLS、日志脱敏、缓存污染、外部源限流/超时、估值时效性、任务重复执行和数据一致性。
4. 性能与可靠性基线：慢查询/索引、N+1、重复请求、缓存层级与失效、并发控制、重试/熔断、健康检查、指标/告警、备份与恢复演练缺口。
5. 最小可行目标架构：保留现有兼容性，明确哪些职责应继续留在前端、Node、Python、PostgreSQL 或 Redis；不要以全面重写为默认方案。
6. 分阶段计划。每项包含收益、影响文件、迁移、接口兼容、测试、灰度、回滚和公网验证；优先选可独立发布、可回滚的小项目。
7. 需要项目所有者决定的事项：生产拓扑、预算/SLO、数据保留、可接受延迟、数据源策略、发布窗口、权限和密钥轮换。

在我明确回复“批准阶段二：<编号>”之前，停在方案阶段。不得写后端代码、创建迁移、修改服务契约、更新 Android、提交部署脚本或发布任何环境。

每次回复固定区分：已验证、代码推断、待生产验证；并列出检查范围、证据、未检查范围、下一步和需我确认的决策。
```

批准阶段二后，每次只实施一个可回滚项目，例如单个高频 API 的超时/缓存/指标改造、单个数据库查询的索引迁移，或单个 OCR 接口的输入边界加固。每项都必须单独给出接口兼容、迁移回滚、本地验证和公网验证计划。
